# 20260714 专业班 Git 基础测试5

### 1. 小王把代码传到了自己的 GitHub，小王最大的错误是什么？（4分）

> 场景：
> 小王在公司实习，觉得公司项目里的一个工具类写得很好，想作为自己简历作品的一部分，于是把部分代码上传到了自己的公开 GitHub 仓库。
> 仓库里没有完整业务代码，但包含：
> 接口地址
> 内部包名
> 部分业务逻辑
> 一个测试用 API Key

**A.** GitHub 不适合放代码

**B.** 未经授权把公司代码上传到公开仓库，可能造成代码和密钥泄露

**C.** 代码不完整，所以没关系

**D.** 只要仓库没人关注就安全

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「公司代码与密钥泄露」的合规风险。✅ A：GitHub 本身适合放代码，问题在于未经授权上传公司代码，而非平台问题。✅ B：把公司项目代码（含内部包名、接口地址、业务逻辑、测试用 API Key）上传到公开仓库，会造成公司代码与密钥泄露，是最大错误，说法正确。❌ C：代码不完整并不代表没有风险，接口地址、内部包名、API Key 同样构成泄露面。❌ D：公开仓库即使没人关注也可能被搜索引擎、爬虫索引，不存在「没人关注就安全」。

---

### 2. AccessKey 配置文件曾提交 GitHub 公开仓库，删除文件并更新 .gitignore 后，最正确、最优先的处置思路是？（4分）

> 背景
> 某 Go 订单服务使用阿里云 OSS 存附件。实习生为方便本地联调，在仓库中新增了生产配置文件并提交 PR，合并后无人 review 密钥字段。次日发现仓库为 GitHub Public。
> 相关片段
> 
> ```yaml
> # configs/prod.yaml（曾提交至 Git，已在最新 commit 删除）
> oss:
>   endpoint: oss-cn-hangzhou.aliyuncs.com
>   access_key_id: LTAI5tXXXXXXXXXXXX
>   access_key_secret: AbCdEfGhIjKlMnOpQrStUvWx
>   bucket: company-order-prod
> 
> 
> ```
> 
> ```gitignore
> # .gitignore（合并删除文件的那次 commit 新增）
> configs/prod.yaml
> .env
> 
> 
> ```
> 现象摘要
> - 当前 main 分支已看不到 configs/prod.yaml，但 Git 历史 commit 中仍存在；
> - 云监控告警：OSS 出现异常高频 PutObject 请求，疑似外部脚本在写垃圾文件；
> - 团队讨论：「文件删了、ignore 加了，应该没事了吧？」
> 结合 密钥泄漏与 Git 历史特性，以下哪项是最正确、最优先的处置思路？

**A.** 立即在云侧禁用/轮换该 AccessKey，审计 OSS 异常访问与账单，并评估 Git 历史暴露面（删文件不能抹掉 history）

**B.** 仅删除文件并更新 .gitignore 即可，Public 改 Private 后风险消除

**C.** 用 git filter-repo 重写历史即可，无需轮换 AccessKey，因为外人 clone 不到最新 main

**D.** 先观察一周 OSS 流量，无异常则不必轮换密钥，避免影响线上服务

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「删文件不能抹掉 Git 历史」与「密钥必须轮换」。✅ A：正确做法是立即在云侧禁用/轮换该 AccessKey，审计 OSS 异常访问与账单，并评估 Git 历史暴露面（git filter-repo 重写历史），说法最完整正确。❌ B：仅删除文件并加 .gitignore，历史 commit 中密钥仍在，Public 改 Private 也消除了已泄露期间的暴露，不能消除风险。❌ C：重写历史能清除仓库内密钥，但密钥已在 Public 期间暴露，必须轮换才能消除泄露，说法错误。❌ D：等待观察期间攻击者可能已利用密钥，不能等一周，应立刻处置。

---

### 3. 内部项目 mirror 到个人 GitHub 公开仓库后，commit author 保留 @wps.cn 邮箱，这类暴露的主要风险是？（4分）

> 背景
> 某 Go 微服务项目原在公司 GitLab。开发者将代码 mirror 到个人 GitHub Public repo 做作品集，未改 commit metadata。
> Git 记录（节选）
> 
> ```text
> commit a1b2c3d4...
> Author: Li Xiaoming <lixiaoming@wps.cn>
> 
>     feat: add payment webhook handler
> 
> 
> ```
> 现象摘要
> - 公开仓库被 GitHub 搜索与邮箱采集爬虫 索引；
> - 攻击者可结合公开资料构造 精准钓鱼；
> - 若该邮箱在其他站点使用 弱密码，存在撞库风险；
> - 领导强调：公司邮箱不应出现在 Public 代码平台。
> 与「代码本身不含密钥」无关，这类 commit 邮箱暴露 的主要风险是什么？

**A.** 增加针对公司域名的精准钓鱼、社工与撞库风险，并带来企业形象与合规问题

**B.** 会直接泄漏仓库内的 AccessKey 明文

**C.** 违反 GPL 协议，GitHub 会自动下架仓库

**D.** 仅影响 GitHub 显示美观，无实际安全后果

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「commit 邮箱暴露带来社工/钓鱼/撞库风险」。✅ A：commit author 保留公司邮箱，攻击者可结合公开资料构造精准钓鱼、社工与撞库，并带来企业形象与合规问题，说法正确。❌ B：邮箱暴露本身不直接泄漏 AccessKey 明文，与「代码不含密钥」不冲突。❌ C：与 GPL 协议无关，GitHub 不会因邮箱自动下架。❌ D：邮箱暴露有实际安全后果（钓鱼/撞库），不只是美观问题。

---

### 4. 同事问：「`git status` 里显示的 `HEAD -> main` 是什么意思？」你查看日志确认当前在 `main`，且没有 detached 状态。在**正常分支开发**下，`HEAD` 当前指向的是？（4分）

**A.** 当前分支 `main` 所指向的最新一次 commit 对象

**B.** 当前仓库第一次 commit，即 `git rev-list --max-parents=0 HEAD`

**C.** 远程 `origin/main` 上的最新 commit，与本地无关

**D.** 工作区里尚未 `git add` 的文件快照

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「HEAD 的指向」。✅ A：在正常分支开发下，HEAD 是一个符号引用，指向当前分支 main，而 main 指向该分支最新的 commit 对象，说法正确。❌ B：HEAD 指向当前分支最新提交，不是仓库第一个 commit（那个是根提交 root commit）。❌ C：HEAD 指向本地 main，与远程 origin/main 无直接关系。❌ D：工作区未 add 的文件是 untracked/modified 状态，与 HEAD 无关。

---

### 5. `.env` 曾提交 Git 后已删除并 gitignore，但未轮换 JWT Secret 与 API Key，「现在看不到文件就没有风险」是否正确？（4分）

> 背景
> 某 Go 用户服务用 .env 加载 JWT 签名密钥与短信 API Key。项目 2023 年曾短暂 Public，后改 Private，历史 commit 仍在。
> .env 历史内容（commit 9f8e7d6）
> 
> ```env
> JWT_SECRET=prod-jwt-signing-key-do-not-share
> SMS_API_KEY=sk_live_xxxxxxxxxxxxxxxx
> DB_PASSWORD=mysql_prod_pass_old
> 
> 
> ```
> 当前状态
> 
> ```gitignore
> .env
> .env.*
> !.env.example
> 
> 
> ```
> 
> ```bash
> git log --all --full-history -- .env
> # 仍可见 2023 年提交
> 
> 
> ```
> 现象摘要
> - 外部研究员称在 Git history 中看到 live API key；
> - 团队已删文件 + gitignore，但 未作废/轮换 密钥；
> - 有人认为改 Private 后绝对安全。
> 「现在仓库里已经看不到了，所以密钥没有泄漏风险」是否正确？最还应补充哪项动作？

**A.** 正确；删除文件并 gitignore 后，密钥不再存在于任何位置

**B.** 错误；须立即轮换 JWT Secret、SMS API Key、DB 密码等所有历史出现过的 Secret，并评估 Public 期间与 fork 的暴露面

**C.** 错误；但只需把仓库改为 Private，无需轮换密钥

**D.** 正确；只要 main 分支无 .env，攻击者无法 checkout 到密钥

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「删文件+gitignore 不能消除已提交的密钥，必须轮换」。✅ B：.env 曾提交且历史 commit 中仍可见，删除文件与 gitignore 只影响未来提交，须立即轮换 JWT Secret、SMS API Key、DB 密码等所有历史出现过的 Secret，并评估 Public 期间与 fork 的暴露面，说法正确。❌ A：密钥仍在 Git 历史中，说「不再存在于任何位置」错误。❌ C：改 Private 不能消除已 Public 期间的泄露，仍需轮换。❌ D：历史 commit 可被 checkout，main 无 .env 不等于攻击者无法获取，错误。

---

### 6. 同事把 node_modules 提交到 Git 后仓库体积暴涨，最合理的处理与预防方式是？（4分）

> 背景
> 实习生首次参与 React 项目，执行 npm install 后把 node_modules/ 一并 git add 提交，MR 显示 数万文件、体积数百 MB。
> 项目结构（节选）
> 
> ```text
> user-admin/
> ├── package.json
> ├── package-lock.json
> ├── node_modules/   ← 被误提交
> └── src/
> 
> 
> ```
> 现象摘要
> - package.json 已记录 react、vite、axios 等依赖；
> - 其他同事 clone 后本可通过 npm install 还原依赖；
> - 团队规范：node_modules 自动生成、不手改、不提交 Git。
> 最合理的处理与预防方式是？

**A.** 继续提交 node_modules，这样同事不用 npm install，加快协作

**B.** 删除 node_modules 后改用 CDN 引入所有依赖，不再使用 npm

**C.** 将 node_modules 从版本库移除并加入 .gitignore

**D.** 只提交 node_modules 里 react 和 antd 两个目录即可

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题核心是「node_modules 不应提交 Git」。✅ C：node_modules 由 npm install 自动生成、体积巨大，应将其从版本库移除并加入 .gitignore，说法正确。❌ A：提交 node_modules 会让仓库体积暴涨、diff 无意义，且 package-lock.json 已能还原依赖。❌ B：用 CDN 引入所有依赖会破坏模块化与版本管理，不合理。❌ D：只提交部分目录是错误做法，node_modules 根本不应进入版本库。

---

### 7. 你执行 `git add src/api.ts` 后，发现把本地调试日志也暂存了。在**不丢弃工作区修改**的前提下，只想把该文件从暂存区撤出。当前最合适的操作是？（4分）

**A.** 对 `src/api.ts` 执行 `git reset HEAD`（或 `git restore --staged`）

**B.** 对 `src/api.ts` 执行 `git checkout -- .` 丢弃整个工作区改动

**C.** 执行 `git stash`，把暂存区和工作区改动一并隐藏

**D.** 执行 `git commit --amend`，把误暂存内容并入上一次提交

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「把已暂存文件撤回暂存区且保留工作区改动」。✅ A：git reset HEAD <file>（或 git restore --staged <file>）把文件从暂存区撤出，保留工作区修改，符合要求。❌ B：git checkout -- . 丢弃整个工作区改动，不符合「不丢弃工作区修改」。❌ C：git stash 会把暂存区和工作区改动一并隐藏，不符合「保留在工作区」。❌ D：git commit --amend 修改上一次提交，与撤出暂存区无关。

---

### 8. 团队评审 long-running feature 合入 `main` 的方案：`feature` 已分叉 40 个 commit，要求**保留「何时从 main 分出」的分叉结构**，便于日后审计发布窗口。在下列机制差异中，**最符合该约束**的是？（4分）

**A.** `git merge` 会生成合并提交，保留分叉与汇合节点

**B.** `git rebase` 会生成合并提交，保留分叉与汇合节点

**C.** `git merge` 会把 feature 提交改写为线性序号，抹平分叉

**D.** `git rebase` 只改变远程跟踪分支，不改变本地历史图

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「merge 保留分叉结构 vs rebase 抹平分叉」。✅ A：git merge 会生成合并提交，保留 feature 从 main 分出的节点与汇合节点，便于审计发布窗口，符合约束。❌ B：rebase 会改写 feature 提交、把分叉变线性，不会保留分叉结构。❌ C：merge 恰恰保留分叉，说「抹平分叉」错误。❌ D：rebase 会重写本地提交历史图，不只是改变远程跟踪分支。

---

### 9. Git合并冲突后如何取消合并（4分）

**A.** 使用 git merge --abort 命令

**B.** 使用 git checkout --detach 命令

**C.** 使用 git revert --merge 命令

**D.** 使用 git reset --merge 命令

**正确答案：** A　|　**我的答案：** C　|　❌

**答案详解：** 本题核心是「合并冲突后取消合并」。✅ A：git merge --abort 用于合并冲突时放弃合并、恢复到合并前的状态，正确。❌ B：git checkout --detach 用于进入分离头指针状态，与取消合并无关。❌ C：git revert --merge 用于撤销已完成的合并提交（merge commit），不是取消进行中的合并。❌ D：git reset --merge 用于重置，但不是取消合并的标准命令，git merge --abort 才是。

---

### 10. Git 提交规范来说，表示“修复bug”一般用哪个前缀？（3分）

**A.** feat

**B.** fix

**C.** perf

**D.** chore

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「Conventional Commits 提交规范前缀」。✅ B：fix 前缀表示「修复 bug」，符合提交规范。❌ A：feat 表示「新功能」。❌ C：perf 表示「性能优化」。❌ D：chore 表示「杂务/构建等辅助改动」。

---

### 11. 关于git log命令，下面哪个选项是正确的？（3分）

**A.** 可以通过 git log --graph 命令来查看所有文件的详细修改内容

**B.** 可以通过 git log --stat 命令来查看每次提交的文件修改统计信息

**C.** 可以通过 git log --decorate 命令来回退到指定的历史提交版本

**D.** 可以通过 git log --grep 命令来查看提交的图形化分支结构图

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「git log 各选项作用」。✅ B：git log --stat 显示每次提交的文件修改统计信息（增删行数、文件列表），正确。❌ A：git log --graph 显示提交历史的图形化分支结构，不是文件详细修改内容。❌ C：git log --decorate 在提交旁显示分支/标签引用，不是回退版本（回退用 git reset）。❌ D：git log --grep 按提交信息搜索，不是图形化分支图（那是 --graph）。

---

### 12. 关于git blame命令，下面哪个选项是错误的？（3分）

**A.** 可以通过git blame命令查看每一行代码是由谁提交的

**B.** git blame命令可以显示当前分支的修改历史记录

**C.** git blame命令可以显示每一行代码的最后修改时间

**D.** 可以通过git blame命令查看每一行代码是在哪个提交中修改的

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「git blame 的功能」。本题要求选出「错误」的说法。✅ A：git blame 可以查看每一行代码由谁（哪个作者）提交，说法正确。❌ B：git blame 逐行显示每行最后修改的提交与作者，而不是「当前分支的修改历史记录」——这是 git log 的功能，说法错误，是本题答案。✅ C：git blame 显示每一行代码的最后修改时间，正确。✅ D：git blame 显示每一行代码是在哪个提交中修改的（commit hash），正确。

---

### 13. Git rebase操作是什么意思？（3分）

> 以下关于Git rebase操作的说法，哪个是错误的？

**A.** 可以将一个分支上的修改合并到另一个分支上

**B.** 可以使提交历史更整洁

**C.** 可以避免合并冲突

**D.** 可以用于删除某些提交记录

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题核心是「git rebase 的操作含义」。本题要求选出「错误」的说法。✅ A：rebase 可以将一个分支的修改（提交）移植到另一个分支之上，说法正确。✅ B：rebase 使提交历史更整洁（线性化），说法正确。❌ C：rebase 在应用提交时仍可能产生冲突，需要手动解决，不能「避免合并冲突」，说法错误，是本题答案。✅ D：rebase -i 交互模式可以删除/合并某些提交记录，说法正确。

---

### 14. Git冲突解决方式中，使用哪个命令可以查看冲突的文件细节？（3分）

**A.** git status

**B.** git diff

**C.** git log

**D.** git checkout

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「查看冲突文件细节」。✅ B：git diff 可以查看冲突文件的具体冲突内容（冲突标记 <<<<<<< / ======= / >>>>>>> 及双方改动），正确。❌ A：git status 只显示哪些文件有冲突，不显示冲突细节。❌ C：git log 查看提交历史，与冲突细节无关。❌ D：git checkout 用于切换分支或恢复文件，不是查看冲突细节。

---

### 15. Git中如何查看某个文件的修改历史（3分）

**A.** git log file.txt

**B.** git blame file.txt

**C.** git diff file.txt

**D.** git status file.txt

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「查看某文件修改历史」。✅ A：git log file.txt 查看该文件的提交历史（每次对该文件的修改），正确。❌ B：git blame file.txt 逐行显示每行最后修改人，不是「修改历史列表」。❌ C：git diff file.txt 查看工作区与暂存区/HEAD 的差异，不是历史。❌ D：git status file.txt 显示文件当前状态，不是修改历史。

---

### 16. 你在本地改完 `README.md` 和 `src/index.ts`，需要把改动推到 GitHub 的 `origin/main`，团队要求 MR 前本地必须有完整 commit。在**正确顺序**下，应执行的操作链是？（3分）

**A.** `git add` → `git commit` → `git push`

**B.** `git commit` → `git add` → `git push`

**C.** `git push` → `git add` → `git commit`

**D.** `git add` → `git push` → `git commit`

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「git add → commit → push 的操作顺序」。✅ A：先 git add 暂存 → git commit 生成提交 → git push 推送，是正确顺序。❌ B：必须先 add 再 commit，顺序颠倒。❌ C：push 必须在 commit 之后，不能先 push。❌ D：push 必须在 commit 之后，顺序错误。

---

### 17. 你刚在本地 `git commit` 了一次，**尚未 push**，发现 commit message 写错且多 add 了一个测试文件。需要撤销这次 commit，但**保留所有改动在暂存区**以便重新提交。在当前约束下，最合适的是？（4分）

**A.** 执行 `git reset --soft HEAD~1`，回退指针并保留暂存区

**B.** 执行 `git checkout HEAD`，丢弃工作区所有未提交修改

**C.** 执行 `git pull origin main`，用远程历史覆盖本地提交

**D.** 执行 `git merge --abort`，终止一次并未发生的合并

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「撤销未推送的 commit 且保留改动到暂存区」。✅ A：git reset --soft HEAD~1 把 HEAD 回退一个提交，保留工作区与暂存区改动，可重新 add/commit，最合适。❌ B：git checkout HEAD 丢弃工作区未提交修改，会丢失改动。❌ C：git pull 会合入远程内容，不是撤销本地提交的正确方式。❌ D：git merge --abort 取消合并，此处没有进行中的合并。

---

### 18. 在Git中，如果想查看当前工作区与暂存区的差异，应使用哪个命令？（4分）

**A.** git diff

**B.** git status

**C.** git commit

**D.** git log

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「查看工作区与暂存区差异」。✅ A：git diff 默认显示工作区与暂存区（index）的差异，正确。❌ B：git status 显示文件状态（modified/staged），不是具体差异内容。❌ C：git commit 创建提交，不查看差异。❌ D：git log 查看提交历史。

---

### 19. GitLab CI 用 echo 打印 DATABASE_URL 到 job log，这属于哪类问题？最合理的整改是？（4分）

> 背景
> 某 Go 服务 GitLab CI 跑集成测试。为排查连库失败，在 .gitlab-ci.yml 临时加了 debug 输出。
> .gitlab-ci.yml（节选）
> 
> ```yaml
> integration_test:
>   stage: test
>   script:
>     - echo "DEBUG DATABASE_URL=$DATABASE_URL"
>     - go test ./... -tags=integration
>   variables:
>     DATABASE_URL: "postgres://ci_user:CI_DB_P@ss@postgres:5432/app_test?sslmode=disable"
> 
> 
> ```
> 现象摘要
> - Job log 对 项目所有 Member 可见，含外包只读账号；
> - GitLab 保留历史 job log，删除 echo 后旧 log 仍在；
> - 变量虽可设 Masked，但 echo 仍可能把 Secret 写入 log；
> - 规范：Secret 不得出现在 CI 日志。
> 这属于哪类敏感信息泄漏？最合理的整改是什么？

**A.** 属于构建缓存未命中；应改用更大的 Runner 机器

**B.** 属于 Go test 失败；应把密码硬编码进测试代码避免环境变量

**C.** 无风险；Base64 编码后再 echo 即可安全输出

**D.** 属于 CI 日志凭证泄漏；立即移除 echo、轮换 DATABASE_URL 密码，Secret 改用 Protected/Masked Variables 且禁止打印

**正确答案：** D　|　**我的答案：** D　|　✅

**答案详解：** 本题核心是「CI 日志中的凭证泄漏」。✅ D：echo 打印 DATABASE_URL 属于 CI 日志凭证泄漏，Job log 对项目成员可见且旧 log 保留，正确整改是立即移除 echo、轮换 DATABASE_URL 密码、Secret 改用 Protected/Masked Variables 且禁止打印，最完整正确。❌ A：与构建缓存未命中无关。❌ B：把密码硬编码进测试代码会进一步泄露，错误。❌ C：Base64 只是编码不是加密，log 中仍可解码还原，有风险。

---

### 20. 功能开发到一半需要紧急修 Bug，最规范的 Git 操作流程是什么？（4分）

**A.** 直接在当前 `feature/user-profile` 分支上修复 bug，提交后告诉组长已修复，然后继续开发用户资料页。这样只在一个分支上操作，避免了分支切换的复杂度，减少出错的可能性。

**B.** 把当前改动全部复制到一个文本文件里备份，然后 `git checkout main` 切到主分支修 bug，修完后再切回来，手动把备份的代码粘贴回去继续开发。

**C.** 执行 `git stash` 暂存当前改动 → 切换到 `main` 分支并拉取最新代码 → 新建 `hotfix/xxx` 分支修复 bug → 提交并推送 → 切回 `feature/user-profile` → 执行 `git stash pop` 恢复之前的改动，继续开发。

**D.** 先把当前未完成的代码 `git commit -m "临时提交"` → 切换到 `main` 修 bug → 修完后切回来继续开发。虽然会多一条临时 commit，但至少代码不会丢，后续可以用 `git rebase -i` 合并提交记录。

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题核心是「功能开发中途紧急修 Bug 的规范流程」。✅ C：git stash 暂存当前改动 → 切 main 拉最新 → 新建 hotfix 分支修复 → 提交推送 → 切回 feature → git stash pop 恢复，是规范完整流程，正确。❌ A：在当前功能分支上直接修 bug 会让 bug 修复混入功能分支，且无法独立发布/评审，不规范。❌ B：手动复制备份代码极易出错、易丢失，不规范。❌ D：git commit -m "临时提交" 会产生无意义提交记录，虽可用 rebase -i 合并，但不如 stash 干净，不是最规范。

---

### 21. 在使用git rebase时，下列哪个操作是正确的？（3分）

**A.** 在已经推送到远程的公共分支上进行rebase

**B.** 在本地未推送的feature分支上进行rebase

**C.** 将主分支的提交记录变基到当前分支头部

**D.** 忽略rebase过程中的所有冲突

**正确答案：** B　|　**我的答案：** C　|　❌

**答案详解：** 本题核心是「git rebase 的正确使用场景」。✅ B：在本地未推送的 feature 分支上进行 rebase 是安全且正确的（尚未共享，改写历史无副作用）。❌ A：在已推送的公共分支上 rebase 会改写共享历史，造成其他协作者冲突，错误。❌ C：正确做法是把当前分支变基到主分支之上，而非把主分支变基到当前分支头部，说法颠倒。❌ D：rebase 过程中的冲突必须解决，不能忽略，错误。

---

### 22. Git amend用于什么场景？（3分）

**A.** 用于删除最后一次提交

**B.** 用于更改最后一次提交的信息

**C.** 用于创建新的分支

**D.** 用于合并分支

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题核心是「git amend 的用途」。✅ B：git commit --amend 用于更改最后一次提交的信息（message）或追加改动到最后一次提交，正确。❌ A：amend 不能删除提交，它修改的是最近一次提交。❌ C：创建新分支用 git branch/switch -c，与 amend 无关。❌ D：合并分支用 git merge。

---

### 23. 如何创建一个空的Git仓库？（3分）

**A.** git init

**B.** git clone

**C.** git remote add

**D.** git new

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题核心是「创建空 Git 仓库」。✅ A：git init 在当前目录初始化一个空的 Git 仓库，正确。❌ B：git clone 复制已有仓库，不是创建空仓库。❌ C：git remote add 添加远程仓库地址，不是创建。❌ D：git new 不是 Git 命令。

---

### 24. 下列关于 Git 描述不正确的是？（3分）

**A.** Git是一个分布式的版本控制工具

**B.** 即使离线也可以提交

**C.** 版本历史记录只存储在服务器端

**D.** 可以创建分支

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题核心是「Git 的分布式特性」。本题要求选出「不正确」的说法。✅ A：Git 是分布式版本控制工具，说法正确。✅ B：Git 支持离线提交（commit 在本地完成），说法正确。❌ C：版本历史记录同时存储在本地和远程（分布式），不只存储在服务器端，说法不正确，是本题答案。✅ D：Git 支持创建分支，说法正确。

---

### 1. 下列关于 git reset 与 git revert 的说法中，正确的有？（多选）（5分）

**A.** git reset --soft HEAD~1 会撤销最近一次提交，但保留工作区与暂存区中的改动

**B.** git reset --hard HEAD~1 会丢弃最近一次提交及其在工作区、暂存区中的对应改动

**C.** git revert <提交> 通过新增一次提交来抵消指定提交的修改，更适合已推送到共享分支的历史

**D.** git revert 常用于撤销已推送的提交，其效果与 git reset 类似，主要在工作区与暂存区体现，通常不增加新的 commit 对象

**正确答案：** ABC　|　**我的答案：** ABC　|　✅

**答案详解：** 本题核心是「git reset 与 git revert 的区别」。✅ A：git reset --soft HEAD~1 撤销最近一次提交，保留工作区与暂存区改动，说法正确。✅ B：git reset --hard HEAD~1 丢弃最近一次提交及其工作区、暂存区对应改动，说法正确。✅ C：git revert <提交> 通过新增一次提交抵消指定提交的修改，适合已推送共享分支，说法正确。❌ D：git revert 会产生一个新的 commit 对象（增加提交），不是「不增加新的 commit 对象」，说法错误。故答案为 ABC。

---

### 2. 下列关于 Git 分支与远程的说法中，正确的有？（多选）（5分）

**A.** git branch 可列出本地分支；加 -a 常可一并查看远程跟踪分支

**B.** git switch -c feature（或 git checkout -b feature）可创建并切换到新分支 feature

**C.** 将本地 main 推送到远程并建立跟踪，常用 git push -u origin main

**D.** git merge 无法合并两个本地分支名，至少有一侧需写成 origin/... 远程跟踪形式

**正确答案：** ABC　|　**我的答案：** ABC　|　✅

**答案详解：** 本题核心是「Git 分支与远程操作」。✅ A：git branch 列出本地分支，加 -a 一并查看远程跟踪分支，说法正确。✅ B：git switch -c feature 或 git checkout -b feature 创建并切换新分支，说法正确。✅ C：git push -u origin main 推送并建立跟踪关系，说法正确。❌ D：git merge 完全可以合并两个本地分支（如 git merge feature），无需写 origin/...，说法错误。故答案为 ABC。

---

### 3. Git Stash 命令在以下哪几种情况下是有用的？（5分）

**A.** 当你需要切换分支但工作目录不干净时可以使用 stash 来保存当前工作进度

**B.** stash 只能在 master 分支中有效

**C.** 使用 git stash apply 恢复最近的 stash 之后，该 stash 会自动从列表中删除

**D.** 通过 git stash list 可以查看所有存储的 stash 记录

**正确答案：** AD　|　**我的答案：** AD　|　✅

**答案详解：** 本题核心是「git stash 的适用场景」。✅ A：切换分支但工作目录不干净时，可用 stash 保存当前工作进度，说法正确。❌ B：stash 在所有分支都有效，不限于 master，说法错误。❌ C：git stash apply 恢复后 stash 默认保留（不会自动删除），需 git stash drop 才删除，说法错误。✅ D：git stash list 查看所有 stash 记录，说法正确。故答案为 AD。

---

