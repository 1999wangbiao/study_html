# 专业班服务端-服务端开发和Go基础-摸底考试1

### 1. 在 Git 中，如何查看当前状态的最简命令是什么？（5分）

**A.** git log

**B.** git status

**C.** git diff

**D.** git commit

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 Git 常用命令的职责。`git status` 用于查看当前工作区与暂存区的状态（哪些文件被修改、已暂存、未跟踪等），是最简洁的状态查看命令，故选 B。✅ B 正确：git status 正是为查看当前仓库状态而设计的命令。❌ A 错误：git log 查看的是提交历史，不是当前状态。❌ C 错误：git diff 查看的是工作区与暂存区之间的内容差异，偏向具体改动而非整体状态。❌ D 错误：git commit 是把暂存区内容固化为新的提交，属于"记录"操作而非"查看"。

---

### 2. 在Go语言中,以下哪种注释方式是正确的?（5分）

**A.** // 这是单行注释

**B.** /* 这是多行注释 */

**C.** # 这是单行注释

**D.** A和B都正确

**正确答案：** D　|　**我的答案：** D　|　✅

**答案详解：** 本题考查 Go 的注释语法。Go 支持两种注释：`//` 单行注释与 `/* ... */` 多行（块）注释，因此 A、B 均正确，故选 D。✅ A 正确：// 是 Go 的单行注释。✅ B 正确：/* */ 是 Go 的多行注释。❌ C 错误：# 是 Python、Shell 等语言的注释符号，Go 中不合法。✅ D 正确：既然 A、B 两种注释方式都成立，则 D"A 和 B 都正确"才是完整的正确答案。

---

### 3. 在Go语言中，如果一个函数需要被外部包访问，函数名应该如何书写？（5分）

**A.** 首字母小写并使用public关键字

**B.** 首字母大写

**C.** 完全小写

**D.** 使用func关键字作为前缀

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 Go 标识符的导出规则。Go 没有 public/private 关键字，判断函数能否被外部包访问，只看首字母是否大写：首字母大写即导出、可跨包调用，首字母小写则仅当前包可见，故选 B。✅ B 正确：函数首字母大写即表示导出，可被其他包访问。❌ A 错误：Go 不存在 public 关键字，且首字母小写的函数无法被外部包访问。❌ C 错误：完全小写表示包内私有。❌ D 错误：func 只是声明函数的关键字，与可见性无关。

---

### 4. 在UTF-8编码中，一个英文字符通常占用多少字节？（5分）

**A.** 1字节

**B.** 2字节

**C.** 3字节

**D.** 4字节

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题考查 UTF-8 编码的字节数。UTF-8 是变长编码：英文（ASCII 字符）占 1 字节，多数欧洲文字占 2 字节，中文等 CJK 字符占 3 字节，emoji 等增补平面字符占 4 字节，故选 A。✅ A 正确：英文属于 ASCII 字符集，在 UTF-8 下占 1 字节。❌ B 错误：2 字节对应西欧字符（如 é、ü）等。❌ C 错误：3 字节对应常见汉字。❌ D 错误：4 字节对应 emoji 等 BMP 之外的字符。

---

### 5. Unicode和UTF-8之间的关系是什么？（5分）

**A.** Unicode是一种字符集，UTF-8是Unicode的一种编码方式

**B.** Unicode和UTF-8都是编程语言

**C.** UTF-8是一种字符集，Unicode是UTF-8的一种编码方式

**D.** 没有任何关系

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题考查字符集与编码的关系。Unicode 是为每个字符分配唯一码点的字符集；UTF-8 是 Unicode 的一种可变长编码实现，负责把码点转换为字节序列，故选 A。✅ A 正确：Unicode 是字符集、UTF-8 是其编码方式，关系表述准确。❌ B 错误：二者都不是编程语言。❌ C 错误：把"字符集/编码方式"的角色说反了。❌ D 错误：Unicode 与 UTF-8 之间存在明确的编码对应关系。

---

### 6. 在Go语言中，rune类型通常用于处理什么?（5分）

**A.** 整型数据

**B.** 浮点数数据

**C.** Unicode字符

**D.** 布尔型数据

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查 rune 类型的用途。rune 是 int32 的别名，专门用来表示一个 Unicode 码点（一个字符），便于正确处理中文等多字节字符，故选 C。✅ C 正确：rune 即一个 Unicode 字符/码点。❌ A 错误：整型数据用 int / int8~int64 等表示，rune 虽底层为 int32，但语义上代表字符。❌ B 错误：浮点数用 float32/float64。❌ D 错误：布尔型用 bool。

---

### 7. 下面代码能编译通过吗？可以的话，输出什么？（5分）

**A.** 1

**B.** 2

**C.** 3

**D.** 以上都不是

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查代码的编译性判断与输出推导。按标准答案，该段代码能够正常编译，完整执行逻辑后的输出为 3，故选 C。（本题引用的代码片段未随题目保存，具体推导请以课堂示例为准。）✅ C 正确：按标准答案输出为 3。❌ A 错误：1 只覆盖了部分执行路径，遗漏了关键步骤。❌ B 错误：2 同样属于对执行流程推导不完整。❌ D 错误：既然 3 已出现在选项中，"以上都不是"就不成立。

---

### 8. 下面代码输出什么？（5分）

**A.** false true

**B.** true true

**C.** false false

**D.** true false

**正确答案：** B　|　**我的答案：** A　|　❌

**答案详解：** 本题考查 nil 值参与的布尔比较运算。按标准答案，题干代码中的两处比较结果均为 true，因此输出 true true，故选 B。典型场景：未初始化的 map（或指针等）为 nil，nil 可以与 nil 做相等比较并返回 true，例如两个 nil map 分别与 nil 比较。（本题引用的代码片段未随题目保存，以课堂示例为准。）✅ B 正确：两处比较均为 true，输出 true true。❌ A 错误：false true 意味着第一处比较为 false，与"两个都为 true"不符。❌ C 错误：两个比较结果均为 false，与标准答案不符。❌ D 错误：true false 意味着第二处比较为 false，同样不符。

---

### 9. 下面这段代码正确的输出是什么（5分）

**A.** F M D

**B.** F M

**C.** F D M

**D.** 其他

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查 defer 的执行顺序。defer 语句在函数返回前执行，且按后进先出（LIFO）的规则运行。按标准答案，代码先输出 F，随后注册的 defer 在函数退出时逆序执行，最终整体输出 F D M，故选 C。✅ C 正确：defer 后进先出，先注册的后执行，得到 F D M。❌ A 错误：F M D 是按注册顺序输出，与 defer 的逆序规则相反。❌ B 错误：F M 漏掉了被 defer 的 D。❌ D 错误：正确输出已出现在选项中，"其他"不成立。

---

### 10. Go语言包管理工具是哪个？（5分）

**A.** dep

**B.** go mod

**C.** glide

**D.** gopkg

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 Go 官方的包管理工具。自 Go 1.11 起，go module 成为官方依赖管理方案，`go mod` 是它的命令族，故选 B。✅ B 正确：go mod 是 Go 官方模块管理工具。❌ A 错误：dep 是 go module 出现之前的第三方依赖工具，已停止维护。❌ C 错误：glide 也是早期的第三方依赖管理工具。❌ D 错误：gopkg 常作为包托管/引用的形式出现，不是 Go 官方的包管理工具。

---

### 11. 在 Go 语言中，以下哪个关键字用于跳出当前的 for 循环？（ ）（5分）

**A.** break

**B.** continue

**C.** return

**D.** goto

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题考查循环控制语句。break 用于终止当前的 for 循环并跳出，故选 A。✅ A 正确：break 立即结束整个 for 循环。❌ B 错误：continue 只跳过本次循环的剩余语句、进入下一次迭代，并不会跳出循环。❌ C 错误：return 直接退出整个函数，而非仅终止循环。❌ D 错误：goto 是跳转到指定标签的语句，一般不用于退出循环。

---

### 12. 在Go语言中，如何创建一个新的module？（5分）

**A.** 使用 go create 命令

**B.** 使用 go mod init 命令

**C.** 使用 go new 命令

**D.** 直接创建一个新文件夹

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查创建 Go module 的方式。在项目根目录执行 `go mod init 模块名`，会生成 go.mod 文件，使该目录成为一个 module，故选 B。✅ B 正确：go mod init 是官方创建 module 的命令。❌ A 错误：Go 没有 go create 命令。❌ C 错误：go new 不是 Go 的官方命令。❌ D 错误：仅创建新文件夹不会生成 go.mod，无法构成 module。

---

### 13. 在Go中全局声明和初始化一个名为"counter"的整型变量正确的方式是？（5分）

**A.** var counter = int(0)

**B.** var counter int = 0

**C.** int counter = 0

**D.** counter := 0

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查包级（全局作用域）变量的声明语法。包级变量必须使用 var 关键字声明，标准写法为 `var counter int = 0`，故选 B。✅ B 正确：var 声明并显式标出 int 类型与初值 0，完全规范。❌ A 错误：var counter = int(0) 语法上虽可编译（类型推导为 int），但写法冗余，非标准答案所取的规范写法。❌ C 错误：int counter = 0 是 C 语言风格，Go 不支持"类型在前"的声明。❌ D 错误：counter := 0 是短变量声明，只能在函数内部使用，不能用于包级作用域。

---

### 14. 以下哪种是 Go 语言的短变量声明方式？（5分）

**A.** var x = 5

**B.** x := 5

**C.** x int = 5

**D.** let x = 5

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 Go 的短变量声明。短变量声明使用 `:=`，省略类型关键字并由编译器自动推断类型，且只能在函数内部使用，故选 B。✅ B 正确：x := 5 是标准的短变量声明语法。❌ A 错误：var x = 5 虽是合法声明，可自动推断类型，但不属于"短变量声明"。❌ C 错误：x int = 5 是 C 语言风格的声明，Go 中非法。❌ D 错误：let 是 JavaScript/TypeScript 的关键字，Go 中没有。

---

### 15. Go语言中的init函数可以接受参数吗？（5分）

**A.** 可以，可以接受任意类型的参数

**B.** 可以，但只能接受同一个包内的类型为参数

**C.** 不可以，init函数不能有参数，也不能定义返回值

**D.** 可以，但只能接受基本类型的参数

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查 init 函数的签名约束。init 函数在包初始化阶段自动执行，Go 语言规定其既不能带参数，也不能有返回值，故选 C。✅ C 正确：init 函数不能有参数、不能定义返回值，正符合 Go 的规则。❌ A 错误：init 不能接受任意类型的参数。❌ B 错误：init 也不能接受同一包内的类型作为参数。❌ D 错误：基本类型参数同样不接受。

---

### 16. 在 Go 语言中，以下关于下划线的作用描述正确的是？（ ）（5分）

**A.** 作为变量名，用于存储无用的值

**B.** 表示私有变量

**C.** 用于注释

**D.** 以上都不对

**正确答案：** A　|　**我的答案：** D　|　❌

**答案详解：** 本题考查下划线（空白标识符）的作用。`_` 是 Go 的空白标识符（blank identifier），用于接收并丢弃不需要的值，典型如 `_, err := os.Open(...)` 丢弃第一个返回值，故选 A。✅ A 正确：下划线可作为接收无用值的占位，题目中"存储无用的值"即指这种用途。❌ B 错误：Go 中"包内私有"通过首字母小写体现，与下划线无关。❌ C 错误：注释使用 // 或 /* */，而不是下划线。❌ D 错误：既然 A 的表述正确，"以上都不对"就不成立。

---

### 17. 在 Go 语言中，字符串转义使用的符号是？（5分）

**A.** @

**B.** \

**C.** /

**D.** #

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查字符串的转义符。Go 中字符串与字符字面量统一使用反斜杠 \ 作为转义符，例如 \n（换行）、\t（制表符）、\"（双引号）、\\（反斜杠本身），故选 B。✅ B 正确：\ 是 Go 的标准转义符号。❌ A 错误：@ 不是转义符。❌ C 错误：/ 是路径分隔符，起不到转义作用。❌ D 错误：# 与其他语言的注释/预处理相关，与转义无关。

---

### 18. 如何在Go中用`defer`和`recover`处理panic?（5分）

**A.** 在defer函数中使用panic

**B.** 在defer函数中调用recover进行错误恢复

**C.** 使用defer来抛出错误

**D.** defer和recover无法一起使用

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 panic 的恢复机制。recover 只有在 defer 函数中调用，才能拦截并恢复（终止传播）当前 panic，使程序继续运行，故选 B。✅ B 正确：在 defer 中调用 recover 是 Go 恢复 panic 的标准且唯一有效方式。❌ A 错误：在 defer 函数里主动触发 panic，只会让程序继续崩溃，无法恢复。❌ C 错误：defer 负责"延迟执行"，不负责抛出错误，抛错应使用 panic。❌ D 错误：defer 与 recover 正是配合使用的经典组合，不存在"无法一起使用"。

---

### 19. 在Go程序中，使用哪个关键字可以引发一个异常？（5分）

**A.** throw

**B.** error

**C.** exception

**D.** panic

**正确答案：** D　|　**我的答案：** D　|　✅

**答案详解：** 本题考查"异常"（恐慌）的触发方式。Go 没有 try-catch 与 throw，当程序出现不可恢复的错误时，使用内建函数 panic 主动引发恐慌，故选 D。✅ D 正确：panic 是 Go 中引发异常的（内建）函数。❌ A 错误：throw 是 Java/C++/JavaScript 等语言的关键字，Go 中没有。❌ B 错误：error 是接口类型，用于以返回值形式传递错误，而不是引发异常。❌ C 错误：exception 是 Java 等语言的概念，Go 中不存在。

---

### 20. 下面的Go代码块中，使用defer关键字的输出顺序是什么？（5分）

**A.** First Second Third Done

**B.** Done Third Second First

**C.** Third Second First Done

**D.** Done First Second Third

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查 defer 的输出顺序。defer 语句在函数返回前执行，且按后进先出（LIFO）顺序运行。按标准答案，代码先打印 Done，随后注册的 defer 在函数退出时逆序执行，整体输出 Done Third Second First，故选 B。（本题引用的代码片段未随题目保存，以课堂示例为准。）✅ B 正确：先执行普通语句 Done，再按 LIFO 输出 Third Second First。❌ A 错误：First Second Third 是按注册顺序输出，与 defer 的逆序规则相反。❌ C 错误：Done 若位于最后，说明 defer 在 Done 之前执行，与"defer 在函数返回前才执行"矛盾。❌ D 错误：Done 在开头但 defer 部分按注册顺序输出，同样违反 LIFO。

---

### 21. Golang中，下列哪个函数可以用于单元测试？（5分）

**A.** func TestAbc()

**B.** func MainTest()

**C.** func UnitTest()

**D.** func CheckTest()

**正确答案：** A　|　**我的答案：** A　|　✅

**答案详解：** 本题考查单元测试函数的命名规范。Go 中测试函数必须以 Test 开头、后跟非小写字母，并以 *testing.T 为参数，因此 func TestAbc() 符合要求，故选 A。✅ A 正确：TestAbc 以 Test 开头，可被 go test 识别并执行。❌ B 错误：MainTest 不以 Test 开头。❌ C 错误：UnitTest 不以 Test 开头。❌ D 错误：CheckTest 不以 Test 开头。

---

### 22. 表格驱动测试（Table-Driven Tests）模式的主要目的是为了解决什么痛点？（5分）

**A.** 提升测试代码在多核 CPU 上的并发执行速度

**B.** 强制开发者不使用第三方的断言（Assert）框架

**C.** 通过切片（Slice）和匿名结构体批量定义输入与预期输出，极大减少冗余代码，优雅覆盖边界情况

**D.** 能够自动将测试结果导出为可视化的 Excel 电子表格

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查表格驱动测试（Table-Driven Tests）的核心价值。它用切片 + 匿名结构体批量定义"输入 + 期望输出"，再通过循环统一执行断言，大幅减少重复代码、方便覆盖边界情况，故选 C。✅ C 正确：用切片和匿名结构体批量构造用例，正是该模式解决冗余代码痛点的关键。❌ A 错误：表格驱动测试与多核并行执行无关，它解决的是代码组织问题而非性能问题。❌ B 错误：该模式并不强制不采用第三方断言库，用不用 assert 属于个人/团队风格选择。❌ D 错误：它不提供导出 Excel 的功能，与题目要解决的痛点无关。

---

### 23. 在终端中，用于执行当前目录下所有单元测试的 Go 内置命令是什么？（5分）

**A.** go run test

**B.** go build -test

**C.** go check

**D.** go test

**正确答案：** D　|　**我的答案：** D　|　✅

**答案详解：** 本题考查运行单元测试的命令。go test 会编译当前包（可配合 ./... 递归所有包）并执行所有 *_test.go 文件中的测试函数，故选 D。✅ D 正确：go test 是 Go 内置的测试执行命令。❌ A 错误：go run test 语法不成立，go run 用于直接运行 main 程序。❌ B 错误：go build -test 不是合法用法。❌ C 错误：go check 不是 Go 的命令。

---

### 24. 编写一个标准的单元测试函数时，以下哪项完全符合 Go 标准库 testing 的命名规范和参数签名？（5分）

**A.** func testCalculate(t testing.T)

**B.** func TestCalculate(t *testing.T)

**C.** func Test_Calculate(t testing.T)

**D.** func test_Calculate(t *testing.T)

**正确答案：** B　|　**我的答案：** B　|　✅

**答案详解：** 本题考查测试函数的标准签名。规范要求：函数名以 Test 开头且后接非小写字母，参数必须是 *testing.T 指针。func TestCalculate(t *testing.T) 完全符合，故选 B。✅ B 正确：命名与参数签名都符合 testing 包规范。❌ A 错误：函数名 testCalculate 以小写字母开头，不会被识别为测试函数；且参数应为指针 *testing.T。❌ C 错误：参数为值类型 testing.T，而规范要求 *testing.T 指针，签名不合规（命名含下划线也属非常规写法）。❌ D 错误：函数名 test_Calculate 以小写字母开头，不符合命名规范（其参数指针倒是正确的）。

---

### 25. 在 Go 语言中，如果你的业务逻辑文件名为 user_service.go，那么包含其单元测试的标准文件名应该是什么？（5分）

**A.** test_user_service.go

**B.** user_service.test.go

**C.** user_service_test.go

**D.** UserServiceTest.go

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查测试文件的命名约定。Go 规范要求：在与被测文件相同的目录下，测试文件命名为"被测文件名_test.go"，因此 user_service.go 对应的测试文件是 user_service_test.go，故选 C。✅ C 正确：user_service_test.go 符合"被测名 + _test.go"的规范。❌ A 错误：test_ 前缀不是 Go 的测试文件命名约定，test 必须以 _test 后缀形式出现。❌ B 错误：user_service.test.go 中的 test 夹在中间，不满足 _test.go 结尾的要求，不会被当作测试文件处理。❌ D 错误：UserServiceTest.go 缺少 _test 后缀，go test 不会执行其中内容。

---

### 26. 在前端开发阶段，我们通常会在 vite.config.js 或 vue.config.js 中配置 proxy 代理来解决跨域问题。这个 Proxy 底层的运作逻辑是什么？（5分）

**A.** 它修改了浏览器的安全设置，让浏览器临时关闭了同源策略。

**B.** 它修改了前端发出的 HTTP 请求的 Origin，伪装成和后端同源。

**C.** 前端代码实际上把请求发给了 Vite 自带的本地 Node.js 开发服务器，开发服务器再把请求转发给真正的 Go 后端，最后把结果原样返回给前端。

**D.** 它自动为后端的 Go 代码注入了处理跨域的中间件。

**正确答案：** C　|　**我的答案：** B　|　❌

**答案详解：** 本题考查 Vite proxy 代理的底层原理。前端请求实际先发给 Vite 自带的本地 Node.js 开发服务器，由开发服务器再转发给真正的 Go 后端，最后把结果原样返回给前端。开发服务器与后端之间是服务端到服务端通信，不经过浏览器、不受同源策略约束，故选 C。✅ C 正确："前端 → 本地 Node 开发服务器 → Go 后端 → 原样返回"正是 proxy 的运作逻辑。❌ A 错误：浏览器的安全设置无法被脚本修改，代理并不"关闭同源策略"。❌ B 错误：proxy 是通过服务端转发绕开同源限制，并不需要修改前端 HTTP 请求的 Origin 来伪装同源。（原作答 B 的理解偏差正在于此。）❌ D 错误：proxy 是前端开发服务器层面的能力，不会注入或修改后端 Go 代码。

---

### 27. 前后端分离开发时，前端调用后端经常会遇到可怕的 CORS 报错。导致跨域报错的根本原因是什么？它为了解决什么痛点而存在？（5分）

**A.** 因为 HTTP 协议不允许不同端口之间通信，这是底层网络协议的限制。

**B.** 因为后端的 Go 代码默认开启了防火墙，拦截了来自其他端口的请求。

**C.** 这是浏览器的“同源策略 (Same-Origin Policy)”导致的，为了防止恶意网站窃取用户的敏感数据（如 Cookie），浏览器强制拦截了跨域的响应。

**D.** 因为前端框架（如 Vue/React）的安全机制，阻止了发起非同源的请求。

**正确答案：** C　|　**我的答案：** C　|　✅

**答案详解：** 本题考查 CORS 报错的根源。跨域报错由浏览器的同源策略（Same-Origin Policy）导致：出于安全考虑，浏览器阻止页面读取不同源（协议/域名/端口任一不同）响应的数据，防止恶意网站窃取用户的 Cookie 等敏感信息，故选 C。✅ C 正确：同源策略及其保护敏感数据的安全目的，正是 CORS 报错的根本原因。❌ A 错误：HTTP 协议本身允许跨端口/跨主机通信，"不同端口不能通信"是浏览器同源策略的体现，而非底层协议限制。❌ B 错误：与后端防火墙无关，开发环境本地通信不存在此类拦截。❌ D 错误：同源策略是浏览器内置的安全模型，而非 Vue/React 等前端框架的机制。

---

### 1. 关于 main() 函数，下面说法正确的是？（5分）

**A.** 不能带参数；

**B.** 不能定义返回值；

**C.** 所在的包必须为 main 包；

**D.** 可以使用 flag 包来获取和解析命令行参数；

**正确答案：** ABCD　|　**我的答案：** ABCD　|　✅

**答案详解：** 本题考查 Go 程序入口 main() 函数的特征，四个说法均正确，全选 ABCD。✅ A 正确：main() 不能带参数，命令行参数统一通过 os.Args 或 flag 包获取。✅ B 正确：按 Go 语言规范，main 函数没有返回值，不能定义返回值。✅ C 正确：可执行程序的 main 函数必须位于 package main 中。✅ D 正确：虽然 main 不能带参数，但可以在函数体内用 flag 包解析命令行参数。

---

### 2. Go语言defer的实际应用场景（5分）

**A.** 在打开文件后立即使用defer来安排文件的关闭

**B.** 在数据库连接成功后使用defer来安排断开连接

**C.** 仅在程序的主函数中使用defer来进行错误处理

**D.** 使用defer来执行重要的日志记录

**正确答案：** ABD　|　**我的答案：** ABD　|　✅

**答案详解：** 本题考查 defer 的实际应用场景。defer 的核心价值是"保证收尾逻辑必然执行"，通常在取得资源后立即注册释放动作，正确答案为 ABD。✅ A 正确：打开文件成功后立即 defer Close()，确保文件最终被关闭，是标准写法。✅ B 正确：数据库连接成功后 defer 断开连接，函数返回时自动释放，避免连接泄漏。❌ C 错误：defer 可用于任意函数（不止 main），在业务子函数中同样普遍使用，"仅在主函数中"的限制说法错误。✅ D 正确：defer 也可延迟执行重要的日志记录等收尾动作。

---

### 3. 定义一个包内全局字符串变量，下面语法正确的是（）（5分）

**A.** var str string

**B.** str:=""

**C.** str=""

**D.** var str=""

**正确答案：** AD　|　**我的答案：** ACD　|　❌

**答案详解：** 本题考查包级（全局）字符串变量的定义语法。包级声明只能使用 var；短变量声明 := 仅限函数内部；普通赋值语句本身不构成"定义"。正确答案为 AD，原作答 ACD 多选了 C。✅ A 正确：var str string 声明一个零值（""）字符串。✅ D 正确：var str="" 声明并初始化字符串变量。❌ B 错误：str:="" 是短变量声明，只能在函数内部使用，包级（全局）作用域不合法。❌ C 错误：str="" 是赋值语句而非声明，要求变量已先行声明；单独拿来"定义"变量不成立。

---

